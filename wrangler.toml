name = "web"
compatibility_date = "2026-05-02"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "."
not_found_handling = "single-page-application"
# excluded files live in .assetsignore (the "exclude" field is not supported for Workers assets)
